def palindrome(word):

    result = []

    for word in each:

    store = word.lower()

    reverse =  store(::-1)

        if(store == reverse):
            result.append (true)
        else:
            result.append(false)

word = ["madam", "hello", "neon", "racecar"]
print(palindrome(word))


