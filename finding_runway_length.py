#ask for acceleration 
#ask the user for speed 
#use the formula to find the run way lenght


acceleration = float(input("acceleration"))
velocity =  float(input("velocity"))

length =  float((velocity * velocity) / (acceleration * 2))
print (length)
